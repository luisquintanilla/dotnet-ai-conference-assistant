# Conference Pulse — Plan

## Vision

Not a presentation *about* AI — a presentation *that is* AI. The session runs as a web app. No traditional slides. The "presentation" is an interactive web surface where content materializes in real-time — generated by agents, enriched by audience interaction, powered by the Microsoft AI stack.

Discussion/panel format. Speaker provides a topic outline ahead of time. Agents generate polls. Audience interacts. Agents analyze, fetch docs via MCP, ingest into a growing knowledge base, and surface insights.

**The closer**: a Copilot SDK console app says "Summarize this session" and cascades through all six technologies in one observable chain.

---

## Documents

| Document | Purpose |
|----------|---------|
| [prd.md](prd.md) | Product requirements, user stories, personas |
| [architecture.md](architecture.md) | Architecture, project structure, NuGet matrix |
| [implementation-spec.md](implementation-spec.md) | Shovel-ready implementation guide per component |
| [session-outline.md](session-outline.md) | Conference session outline (ingested at startup) |
| `../data/seed-topics.json` | Pre-defined topics & poll prompts |

---

## The Six Technologies

1. **Microsoft.Extensions.AI** — IChatClient, IEmbeddingGenerator, ChatClientBuilder, AIFunctionFactory
2. **Microsoft.Extensions.DataIngestion** — `IngestionPipeline<T>` with readers, chunkers, enrichers, writers
3. **Microsoft.Extensions.VectorData** — InMemoryVectorStore, auto-embedding, SearchAsync for RAG
4. **Microsoft Agent Framework** — 3 ChatClientAgents + 3 workflows via AgentWorkflowBuilder
5. **MCP** — Server (6 tools + 2 resources) AND Client (Microsoft Learn + DeepWiki)
6. **GitHub Copilot SDK** — Console app closer: one command → cascades through all layers

---

## Implementation Order

```
Phase 1: Foundation          Phase 2: Knowledge         Phase 3: Intelligence
  scaffold-solution            ingestion-record-model     agent-tools
  core-models                  ingestion-outline-pipe     survey-architect-agent
  core-services                ingestion-response-pipe    response-analyst-agent
  session-data                 ingestion-mcp-pipe         knowledge-curator-agent
                               semantic-search-service    poll-generation-workflow
                                                          response-analysis-workflow
Phase 4: Bridge              Phase 5: Frontend            session-summary-workflow
  mcp-server-tools             blazor-components
  mcp-server-resources         blazor-layouts           Phase 6: Integration
  mcp-clients                  blazor-presenter           program-cs-wiring
  copilot-demo-app             blazor-session             aspire-apphost
                               blazor-display             readme-docs
```

---

## Design Decisions

- **.NET 10**
- **Blazor Interactive Server** (SignalR built-in, single codebase)
- **In-memory everything** (no external DB — demo runs for one session)
- **Azure OpenAI** (configurable via env vars)
- **MCP Streamable HTTP** transport
- **Real DataIngestion** package (`IngestionPipeline<T>`)
- **.NET Aspire** for observability (OpenTelemetry dashboard)
